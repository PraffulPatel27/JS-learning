// <-------- Insertion on array at the middle -------->

let arr = [1, 4, 5, 7];
console.log (arr);


// Splice method syntax : 
  // array_name.splice (index, howmany, item1, ....., itemX)

// <------ splice method ------>
arr.splice (2, 0, 'a', 'b', 'c');
console.log (arr);
