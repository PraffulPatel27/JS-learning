// <-------- Insertion on array at the middle -------->

let arr = [1, 4, 5, 7, 8, 9, 10, 11];
console.log (arr);


// Splice method syntax : 
  // array_name.splice (index, howmany_delete)

// <------ splice method ------>
arr.splice (2, );
console.log (arr);
